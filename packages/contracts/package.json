{
  "name": "contracts",
  "private": true,
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "deploy": "forge script script/Deploy.s.sol --rpc-url $RPC_URL --private-key $PRIVATE_KEY --broadcast --verify"
  },
  "devDependencies": {
    "forge-std": "^1.1.2",
    "openzeppelin-contracts": "^4.0.0"
  }
}
